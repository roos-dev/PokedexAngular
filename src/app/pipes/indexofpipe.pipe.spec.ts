import { IndexofpipePipe } from './indexofpipe.pipe';

describe('IndexofpipePipe', () => {
  it('create an instance', () => {
    const pipe = new IndexofpipePipe();
    expect(pipe).toBeTruthy();
  });
});
